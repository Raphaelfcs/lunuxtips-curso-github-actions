name: Primeiro Workflow Linuxtips

on:
  workflow_dispatch

jobs:
  giropops:
    runs-on: ubuntu-latest
    steps:
      - name: Confere Versao do ubuntu
        run: cat /etc/os-release
      
      - name: Exibir Uma Mensagem
        run: echo "Estou faazendo o curso de GitHub Actions da LinuxTisp"